services:
  backend:
    build: ./backend
    container_name: apagones_backend
    restart: unless-stopped
    volumes:
      - ./data:/data

  web:
    build: ./frontend
    container_name: apagones_web
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "8098:80"
